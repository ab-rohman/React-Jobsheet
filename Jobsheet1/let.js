if(true){
    // this is if condition when the value is true the code inside { } will be excuted
    let name = 'Framework-based Programming';
    // declaring Variable name with type let (so it can be changed)
    name = 'Politeknik Negeri Malang';
    // change the value of variable name with new value
    alert(name);
    //show the value of name
}